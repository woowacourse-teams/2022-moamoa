spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  profiles:
    include: security

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
